# 阶段 09 开发记录

2026-09-26：开发中，不是整阶段通过。

## 已实现

新增 `scripts/game-long-run.mjs` 和 `quality/longRun.ts`。策略在启动前冻结，持续时间 10 秒至 4 小时；真实键盘输入循环，不调用游戏函数。固定 Web 构建、分辨率、图形档位声明及性能阈值，开始与结束校验源和产物 hash。每 5 秒保存运行状态、引擎帧计数、浏览器可见性、渲染进程工作集；每分钟截图。运行错误、帧计数停止和暂停会中止并保留报告。

浏览器 requestAnimationFrame 间隔与引擎 process frame 速率分开记录，不能当作 GPU 耗时。性能报告保存中位数、p95、p99、长帧比例、内存序列和启动信息。它不自动判断画面相似、需求完成或趣味；这三项显式标成未评估。

## 当前证据

- `npm run verify`：81 文件、652 测试，退出 0（字体和长测策略版本）；后续增量另记录。
- 真 Electron / Godot 工程夹具：`.noobi-private/stage-09/long-run/2026-09-25T20-40-14.024Z/report.json`。运行 15.77 秒，1341 个浏览器帧间隔样本，无运行错误；此初次工具验证尚未包含后来新增的引擎 FPS 门槛。**不是 30 分钟验证，不是自主生成完整游戏**。
- 构建 `44bb6b56-a4c7-4548-8860-08247183d040`；源 `83b02ea01c78912c284c1221db8760dc57c1d8a9a83b8b93f7a250aa60dac637`；产物 `6c558877a79b05fa09a26c6986082a3e8dd0533163e0184377c259d13027a2c3`。

## 使用

先 `npm run build:main`，再用 Electron 运行 `scripts/game-long-run.mjs --store <私有构建仓库绝对路径> --project <项目ID> --build <固定构建ID> --policy <JSON>`。策略字段见 `LongRunPolicy`；30 分钟须 durationSeconds >= 1800。输入步骤只有 key/holdMs 或 waitMs，不能注入脚本。报告在 `.noobi-private/stage-09/long-run/`。

## 未完成

当前构建 30 分钟实际路线、完整流程和冻结设备正常档位实测；参考相近镜头的多维对照入口；故障样例准确性矩阵；至少 3 位真人试玩。全部保留为未验收，不能以工具和短测代替。

## 参考对照入口与长测负例（2026-09-26）

Noobi 检查器新增「参考与实际画面对照」：并排显示原比例参考图、宿主保存的构建截图；可逐项记录风格、镜头、动作、反馈、规则，并查看选定需求与推断/未知。四类结论独立呈现，保存手动记录不会改变交付或真人体验验收。视频参考可选择已抽取关键帧，但不能据静态帧证明连续动作。

参考字节与冻结构建输入、截图与宿主报告 hash 均校验；源码变化时历史画面可查看，但不能保存新对照。记录绑定构建/参考/截图 hash，独立文件持久化并检查完整性。这里的手动记录可以来自开发者，不代表真人玩家的意见。

真实 Electron 工程负例通过官方界面保存一条明确标识的开发者测试记录，页面重载后仍存在。参考是骑士，产物刻意使用工程胶囊；五项判断保持「尚未判断」，未冒充图生模型成功。截图位于 `.noobi-private/stage-09/comparison-04-top.png`，实际对照工程与元数据见 `comparison-fixture-latest.json`。本次不将用户参考图片复制进公开仓库。

长测发现并修复一个漏检：角色掉出地图时引擎仍持续渲染，仅检查帧数会误判。现在策略必须预先冻结活动角色路径、可玩坐标范围和游戏状态断言，越界或状态异常即失败，并保存运行包和截图。

- 原 30 分钟尝试 `long-run/2026-09-25T21-13-21.758Z` 在约 10 分钟发现角色掉落后终止；仅有进度和截图，没有完整结论报告，明确不算通过。SIGTERM 后完整报告写出尚未验证，不承诺强制退出一定生成结论。
- 实际掉落负例 `long-run/2026-09-25T21-24-12.510Z/report.json`：退出 1，坐标 `[0,-9.8195,13.9376]` 被拦截，可靠性/性能/30 分钟覆盖全部 false。
- 修订策略短跑 `long-run/2026-09-25T21-34-19.594Z/report.json`：15 秒目标完成，运行及性能工具检查通过；仍不是 30 分钟或完整生成游戏。
- 最终 `npm run verify` 83 文件 / 660 测试通过，退出 0，日志 `final-verify.log`；`npm run smoke:ui` 退出 0，实际检查 workbench 截图；记录损坏/过期绑定/重载持久化测试通过。

09-D01 已有操作与记录组件，真实成品五维对齐仍待评估；D02–D04 仍保留长流程、代表性设备和真人反馈要求，不整体勾选完成。


## 30 分钟工程长测结果（2026-09-26）

`long-run/2026-09-25T21-35-15.782Z/report.json` 已完整生成：1800.100 秒，reliabilityPassed / performancePassed / thirtyMinuteCoverage 均为 true，无运行错误。策略运行前冻结，真实 D/A 键盘移动及间隔持续执行；每次取样均检查角色、生命和 playing 状态。全程角色 X 范围约 [-1.587, 1.627]、Y=0.000156、Z=3，未掉出声明范围。已实际查看 0 / 881 / 1760 秒截图，角色和场地正常。

环境：Apple M5 Pro、24 GiB 内存、macOS Darwin 25.3.0、arm64、Electron 43.3 / Chromium 150.0.7871.212、Godot 4.7.1，窗口 1280×720。策略明确标注有并发生成工作，不能称为空闲设备基准。记录 216,000 个浏览器 rAF 间隔，中位数 8.3 ms、p95 9.5 ms、p99 10.1 ms、最长 16.6 ms；长帧比 0。引擎帧计数区间最低约 117.3 FPS；去除启动预热后的渲染进程末端内存增长截断值为 0 MiB，不代表系统/GPU 总内存没有变化。

此项证明的是固定工程场景持续输入下的稳定性和工具策略通过。它没有执行多区域路线或 30 分钟内容，**不是自主生成完整游戏的 09-A02 全流程验收**，不能替代美术与真人体验评估。之前的掉落失败仍保留。
