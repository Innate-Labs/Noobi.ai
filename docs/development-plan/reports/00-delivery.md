# 阶段 00 交付与用户验收报告

## 当前状态

- 阶段：00，统一代码基线与回归环境。
- 状态：**已验收**；2026-09-25 用户回复“下一步”，同意进入阶段 01。
- 交付日期：2026-09-25。
- 分支：`codex/stage-00-baseline-20260925`。
- 代码/构建版本：`765b52a`。后续文档提交不改变该应用构建。
- 两个父版本：本地生产增强 `a83ab31`、拉取确认的远端主线 `ccc273e`，均为当前代码祖先。
- 本地已提交，未推送或合并远端。原工作树的代码分支和其他未提交资料保留。
- 环境、回归样例和已有问题见 [环境与回归清单](00-baseline-environment.md)。

## 本阶段完成了什么

| 任务 ID | 实际交付 | 状态 |
|---|---|---|
| 00-D01 | 拉取 origin，确认 main 为 ccc273e，核对本地 a83ab31 差异 | 完成 |
| 00-D02 | 隔离工作树整合两条代码线，解决 6 个文件冲突；David 三项修复完整保留 | 完成 |
| 00-D03 | 固定 Node、npm、Codex、Godot 和模板版本，完成干净依赖安装与真实启动 | 完成 |
| 00-D04 | 建立回归与素材清单；记录依赖告警、历史失败和未验证能力 | 完成 |

冲突处理保留的行为：

- 项目可直接使用用户选择的空文件夹，并保留目录规范化、移动重连与访问/删除互斥。
- Godot 预览与评测绑定冻结构建；不把源目录的素材混入冻结交付版本。
- 输入框保留主线的继续输入/排队交互，同时显示本地断线恢复提示。
- 评测面板保留可折叠抽屉和历史结果语义，显示实际通过的基础检查项数，不将其描述为美术或趣味评分。
- 实现/修复回合保留主线的 60 分钟上限，同时保留本地 90 秒连接恢复机制和工具版本隔离。

本地原提交中已有的图标、素材等随原提交保留；本阶段没有额外制作宣传资产。

## 验证证据

日志和真实项目截图保存在 Git 忽略目录，仅供本地验收；下面链接在本机可用，远端不会含有这些私人数据。

| 验收 ID | 实际命令或操作 | 实际结果 | 证据 |
|---|---|---|---|
| 00-A01 | Node 22.20.0 / npm 10.9.3 执行 `npm ci` | 退出码 0；依赖告警已登记 | [安装日志](../../../.noobi-private/stage-00/npm-ci-node22.log) |
| 00-A01 | `npm run verify` | 退出码 0；60 个测试文件、454 项测试通过，双 TypeScript 检查与构建通过 | [验证日志](../../../.noobi-private/stage-00/verify.log) |
| 00-A01 | `git diff --cached --check` | 退出码 0；冲突标记已清除 | 合并提交 765b52a |
| 00-A02 | `npm run smoke:ui` | 退出码 0；启动/截图/关闭通过，人工查看首页截图通过 | [UI 日志](../../../.noobi-private/stage-00/smoke-ui.log)、[首页截图](../../../.noobi-private/stage-00/workbench.png) |
| 00-A02 | `NOOBI_SMOKE_VIEW=workbench NOOBI_SMOKE_STATUS=completed NOOBI_SMOKE_EXPERIENCE_REPORT=expand` 启动隔离截图模式 | 退出码 0；抽屉 560×585，预览高度保持 601；人工查看通过 | [日志](../../../.noobi-private/stage-00/smoke-report.log)、[抽屉截图](../../../.noobi-private/stage-00/report-expanded.png) |
| 00-A03 | 全量测试覆盖 David 三项回归及本地 production/quality/runtime/connection 测试 | 全部通过；David 修复相关实现与 origin/main 无差异 | verify 日志、环境与回归清单 |
| 补充 | `npm run smoke:godot` | 退出码 0；真实导入、检查、Web 导出与预览通过 | [Godot 日志](../../../.noobi-private/stage-00/smoke-godot.log) |
| 补充 | 启动整合版，读取实际项目，打开文件，再返回首页 | 12 个项目可见；选定项目 27 个文件节点、README 可读，无告警和 JS 异常 | [操作结果](../../../.noobi-private/stage-00/live-app-check.json) |

真实服务调用：没有发起模型生成、素材生成或付费试玩。Codex App Server 已启动并显示就绪；这不替代后续真实生成评测。人工介入包括冲突解决、测试修正与截图检查，均为 Noobi 研发，不是“自主生成游戏成功”记录。

## 请用户这样验收

验收的是整合分支的已构建 Electron 桌面应用，**不使用旧分支的 localhost:5174**。原开发实例已正常退出。当前已为你打开整合版本；也可在该工作树执行：

```sh
./.noobi-private/stage-00/Start-Noobi.command
```

或使用上述固定 Node 环境后执行 `node_modules/.bin/electron .`。准确工作树可通过 `git worktree list` 查找 `codex/stage-00-baseline-20260925`；`git show 765b52a --no-patch` 可确认构建来源。界面没有新增版本标签，不根据窗口外观猜测版本。

| 步骤 | 你的操作 | 应看到的结果 |
|---|---|---|
| 00-U01 | 查看已打开的 Noobi 首页 | 原有项目仍在左侧；首页、设置和输入区正常显示 |
| 00-U02 | 点击一个已有项目，进入“文件”，打开 README，再点击左侧“首页” | 文件内容正常读取，能返回首页，没有阻断错误；原游戏历史状态不被本阶段强制改为成功 |
| 00-U03 | 查看本报告和环境清单 | 能核对整合范围、固定版本、454 项测试结果以及未验证能力 |

阶段 00 不需要点击“开始制作”或消耗模型额度来验收。你可以直接回复“阶段 00 通过”，或给出具体问题，我会在本阶段修复后重新交付。

## 已知问题与边界

依赖 audit 告警、已有项目历史失败和跨平台/完整生成未验证事项见环境清单。本阶段没有新增方案选择、图片/视频理解或完整 3D 制作能力；这些依次在后续阶段实现。

停止应用：使用应用菜单退出或 `Cmd+Q`。代码回退不需要撤销合并：退出整合版后，可回到保留的原工作树运行旧版。本地项目索引在启动前已备份在忽略目录；不自动覆盖索引，避免丢失验收后的新操作。

## 用户验收记录

- 用户结论：通过；用户在验收交付后明确回复“下一步”。
- 反馈、验收日期及返工记录：尚无。

## 下一阶段

[阶段 01：制作方案生成、选择与开始](../01-plan-selection.md)。只有用户明确通过阶段 00 后才开始。
