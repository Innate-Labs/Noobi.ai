# 阶段 08：多区域、任务成长与完整游戏

状态：开发中（已有部分专项/样板，不代表整阶段已完成或已验收）。实际进度以 [STATUS.md](STATUS.md) 为准。

## 目标与入口条件

把样板扩展为有连贯流程和结局的完整单人游戏。

- 前置条件：阶段 07 已由用户验收通过；对应记录可在 STATUS.md 查到。
- 需求追踪：R11 R09，见 [REQUIREMENTS.md](REQUIREMENTS.md)。
- 开发前必读：[README.md](README.md)、[STANDARDS.md](STANDARDS.md)。
- 代码入口（需开工时核实）：游戏组件、GameSpec/LevelGraph、production/、任务/背包/存档组件与生成模板。拟新增服务名称不构成既有实现。

## 开发任务

- [x] 08-D01：实现任务、背包、能力解锁、奖励、保存/继续等可组合组件与状态契约。
- [x] 08-D02：建立区域连接与任务前置图，校验目标可达、钥匙/能力顺序和不可恢复状态。
- [ ] 08-D03：标杆扩展为三个差异区域、主线/可选路线、两项探索能力、多种敌人行为和最终挑战。
- [ ] 08-D04：加入教学、挑战递进、目标提示、结局与完整重试流程；更新内容前检查存档版本。

已勾选表示对应平台实现及工程自验已完成，完整生成作品和人工验收仍按下方 A/U 项及交付报告单独核对。

本轮补充：交互探索型 manifest 装配已通过原生和浏览器工程路线，见 [装配报告](reports/08-game-assembly.md)。它补充 D04 的通用目标/结局/重试连接，未满足 D03 的成品内容，也未将 D04 整体勾选。

## 本阶段开发标准

- 内容扩展必须带来玩法/决策变化，不用复制敌人或拉长步行凑时长。
- 30–60 分钟是待真人验证的目标；本阶段如未达成明确记录，不凭代码量宣称达标。
- 验收标准在开发前冻结；实现、测试与用户体验同步完成。不得靠降低标准使阶段通过。
- 每个任务有实际代码/产物及证据；外部能力和真实模型验证与 mock 测试分开记录。

## 自动验证与真实运行验收

| 编号 | 检查 | 通过标准 |
|---|---|---|
| 08-A01 | 组合规则 | 任务→奖励→背包→开门→保存→重载整链正确；重复领取、死亡丢状态等边界受测。 |
| 08-A02 | 关卡图 | 循环前置、不可达道具、缺少终点和无法恢复路径均能检出；实际几何再由试玩验证。 |
| 08-A03 | 完整新游戏 | 从正常开始跨越全部区域到结局；记录完整输入与构建版本，不能用调试跳关代替。 |
| 08-A04 | 存档兼容 | 各关键进度保存退出再进入；不兼容版本须阻止错误读取并保留原档。 |

代码变更完成后执行仓库要求的 `npm run verify`；涉及界面时执行 `npm run smoke:ui` 并实际检查截图。除此以外，执行上表要求的真实运行测试。命令、环境、退出码、日志位置、构建 ID 写入阶段报告；未运行必须写明，不预填通过。

## 用户验收步骤

验收前，开发者必须提供可运行版本、准确启动方式、示例输入/测试素材及下列操作所需的入口，不能只交代码 diff。

| 编号 | 你的操作 | 应看到的结果 |
|---|---|---|
| 08-U01 | 从新游戏开始游玩三个区域 | 目标清楚，挑战递进，各区域有实质不同。 |
| 08-U02 | 获得能力并回访可选路线 | 能力确实改变探索，奖励和任务相互衔接。 |
| 08-U03 | 中途保存退出再继续，完成结局 | 进度正确，最终挑战与结局可完成。 |

## 交付物

完整标杆构建、内容/任务图、完整流程证据、存档矩阵与实测时长。

- 使用 [REPORT-TEMPLATE.md](REPORT-TEMPLATE.md) 建立 `reports/08-delivery.md`。
- 提供本阶段提交、运行/构建版本、日志与截图或录像链接、已知问题。
- 用户反馈后，将实际结论写入报告并更新 STATUS.md。

## 范围边界与风险

- 本阶段不做：不扩展第二品类，不加入联网多人或大型开放世界。
- 风险处理：多系统组合使修复范围增大，按依赖回归并始终保留上一可玩版本。

## 退出条件与汇报

所有开发任务与适用验收项有证据，开发者自验通过后状态改为“待用户验收”，立即向用户汇报本阶段做了什么、怎么试、验证结果和遗留问题。只有用户明确验收通过，才改为“已验收”并进入下一阶段。存在未完成项时留在本阶段；范围变更必须先由用户明确接受。

用户未回复、等待超时、测试全绿均不等于用户验收通过。禁止提前填写完成日期或用户意见。
