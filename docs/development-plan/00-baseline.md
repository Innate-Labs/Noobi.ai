# 阶段 00：统一代码基线与回归环境

状态：待用户验收。实际进度以 [STATUS.md](STATUS.md) 为准。

## 目标与入口条件

获得同时包含本地生产增强和最新主线修复的可运行开发基线。

- 前置条件：无；从本文件开始。
- 需求追踪：R01，见 [REQUIREMENTS.md](REQUIREMENTS.md)。
- 开发前必读：[README.md](README.md)、[STANDARDS.md](STANDARDS.md)。
- 代码入口（需开工时核实）：package.json、CONTRIBUTING.md、src/main/、src/renderer/、现有测试与 CI。拟新增服务名称不构成既有实现。

## 开发任务

- [x] 00-D01：读取最新 Git 状态和远端 main；规划中的 a83ab31、ccc273e 仅为历史锚点，禁止直接假定仍是最新版本。
- [x] 00-D02：在隔离分支整合本地生产增强与主线，逐项核对社区修复；保留用户现有资料和未提交文件。
- [x] 00-D03：固定 Node、Codex、Godot 与导出模板版本；记录依赖、系统和启动方式。
- [x] 00-D04：建立回归清单和测试素材清单，记录已有失败，不把现有问题悄悄归到后续阶段。

## 本阶段开发标准

- 集成后的功能清单可追溯到提交，冲突按产品最终行为解决。
- 测试数据、生成内容和日志分离；不提交凭据、浏览器配置或用户私人原始响应。
- 验收标准在开发前冻结；实现、测试与用户体验同步完成。不得靠降低标准使阶段通过。
- 每个任务有实际代码/产物及证据；外部能力和真实模型验证与 mock 测试分开记录。

## 自动验证与真实运行验收

| 编号 | 检查 | 通过标准 |
|---|---|---|
| 00-A01 | 统一构建 | 执行 npm ci、npm run verify、git diff --check；全部通过，记录实际测试数。 |
| 00-A02 | UI 启动 | 执行 npm run smoke:ui 并检查截图；正常启动与关闭无阻断错误。 |
| 00-A03 | 集成功能 | David 三项回归与本地生产相关测试均保留并通过。 |

代码变更完成后执行仓库要求的 `npm run verify`；涉及界面时执行 `npm run smoke:ui` 并实际检查截图。除此以外，执行上表要求的真实运行测试。命令、环境、退出码、日志位置、构建 ID 写入阶段报告；未运行必须写明，不预填通过。

## 用户验收步骤

验收前，开发者必须提供可运行版本、准确启动方式、示例输入/测试素材及下列操作所需的入口，不能只交代码 diff。

| 编号 | 你的操作 | 应看到的结果 |
|---|---|---|
| 00-U01 | 启动指定验收版本 | 首页出现，现有项目可见，启动入口和版本明确。 |
| 00-U02 | 打开一个现有项目，再返回首页 | 项目内容可读取，界面没有阻断错误。 |
| 00-U03 | 核对开发基线报告 | 能看到整合范围、运行环境、已知问题及日志入口。 |

## 交付物

集成提交、环境说明、基线功能清单、验证日志与启动截图。

- 使用 [REPORT-TEMPLATE.md](REPORT-TEMPLATE.md) 建立 `reports/00-delivery.md`。
- 提供本阶段提交、运行/构建版本、日志与截图或录像链接、已知问题。
- 用户反馈后，将实际结论写入报告并更新 STATUS.md。

## 范围边界与风险

- 本阶段不做：不在此阶段实现新方案 UI、视频分析或新的游戏组件。
- 风险处理：缺少运行时、登录或供应商配置时记录具体阻塞；可验证部分继续，完整阶段不标通过。

## 退出条件与汇报

所有开发任务与适用验收项有证据，开发者自验通过后状态改为“待用户验收”，立即向用户汇报本阶段做了什么、怎么试、验证结果和遗留问题。只有用户明确验收通过，才改为“已验收”并进入下一阶段。存在未完成项时留在本阶段；范围变更必须先由用户明确接受。

用户未回复、等待超时、测试全绿均不等于用户验收通过。禁止提前填写完成日期或用户意见。

开发者交付记录：[阶段 00 报告](reports/00-delivery.md)。任务勾选表示已实现和自验，不代表用户已通过验收。
