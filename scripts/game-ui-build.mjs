import{app}from'electron';import{mkdir,writeFile}from'node:fs/promises';import{join,resolve}from'node:path';import{ProjectStore}from'../dist/main/projectStore.js';import{GodotBuildStore}from'../dist/main/production/godotBuildStore.js';import{GodotEnvironmentService}from'../dist/main/godotEnvironmentService.js';import{buildGodotCandidate}from'../dist/main/production/godotBuilder.js';import{createGameUiFixture}from'./game-ui-fixture.mjs';
const out=resolve('.noobi-private/game-ui/runtime',new Date().toISOString().replaceAll(':','-'));app.setPath('userData',join(out,'electron'));app.on('window-all-closed',()=>{});
app.whenReady().then(async()=>{try{await mkdir(out,{recursive:true});const projects=new ProjectStore(join(out,'projects.json'),join(out,'games'));await projects.init();const project=await projects.create({name:'Game UI engineering fixture',idea:'UI component validation only',engine:'godot',parentDirectory:join(out,'games')});await createGameUiFixture(project.root);const environment=new GodotEnvironmentService({storageFile:join(out,'godot.json')});await environment.init();const store=new GodotBuildStore(join(out,'builds'));const build=await buildGodotCandidate({projectId:project.id,projectRoot:project.root,store,environment});const result={out,store:store.storageRoot,projectId:project.id,root:project.root,buildId:build.record.buildId,build:build.record};await writeFile(join(out,'build.json'),JSON.stringify(result,null,2));await writeFile('.noobi-private/game-ui/latest-build.json',JSON.stringify(result,null,2));console.log('GAME_UI_BUILD_OK',out);app.exit(0);}catch(e){console.error(e);app.exit(1)}}).catch(e=>{console.error(e);app.exit(1)});
